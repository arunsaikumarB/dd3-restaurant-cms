/**
 * @deprecated Import from `src/config/env` for new code.
 * Re-exports preserved for existing Supabase imports.
 */
export {
  PRODUCTION_SITE_URL_FALLBACK,
  getSiteUrl,
  getSupabaseUrl,
  getSupabaseAnonKey,
  getSupabaseServiceRoleKey,
  isSupabaseConfigured,
  isAdminDevBypassEnabled,
} from "../../config/env";
